<section class="hero">
  <div class="hero__inner">
    <div class="hero__content">
      <p class="hero__eyebrow">Hola, soy Renato</p>

      <h1>Analista de datos</h1>

      <p class="hero__description">
        Desarrollo proyectos prácticos de análisis de datos utilizando Python,
        SQL y Power BI, complementando mi perfil con conocimientos de desarrollo
        web y programación.
      </p>

      <div class="hero__actions">
        <a href="#proyectos" class="btn btn--primary">Ver proyectos</a>

        <a href="#contacto" class="btn btn--secondary">Contactarme</a>

        <a
          href="/documents/Renato_Saldivia_CV_Data_Analyst.pdf"
          class="btn btn--secondary"
          target="_blank"
          rel="noopener noreferrer"
        >
          Descargar CV
        </a>
      </div>
    </div>

    <div class="hero__photo">
      <img src="/images/Renato.jpg" alt="Renato Saldivia" />
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    padding: 2rem;
    padding-top: 7rem;

    background: #0f172a;
    color: white;
  }

  .hero__inner {
    width: min(1200px, calc(100% - 4rem));
    min-height: calc(100vh - 9rem);
    margin: 0 auto;

    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 4rem;
  }

  .hero__content {
    max-width: 700px;
  }

  .hero__eyebrow {
    color: #60a5fa;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 0.5rem;
  }

  h1 {
    font-size: clamp(2.5rem, 8vw, 5rem);
    line-height: 1.05;
    margin: 0;
  }

  .hero__description {
    font-size: 1.1rem;
    line-height: 1.7;
    color: #cbd5e1;
    margin: 1.5rem 0;
  }

  .hero__actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .btn {
    text-decoration: none;
    padding: 0.9rem 1.4rem;
    border-radius: 999px;
    font-weight: 600;
    transition: 0.2s ease;
  }

  .btn--primary {
    background: #2563eb;
    color: white;
  }

  .btn--secondary {
    border: 2px solid #64748b;
    color: white;
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .hero__photo {
    width: clamp(220px, 30vw, 380px);
    aspect-ratio: 1;
    justify-self: center;

    border: 4px solid #2563eb;
    border-radius: 50%;
    overflow: hidden;
  }

  .hero__photo img {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 7rem 1.5rem 2rem;
    }

    .hero__inner {
      width: 100%;
      grid-template-columns: 1fr;
      gap: 3rem;
    }

    .hero__content {
      text-align: center;
    }

    .hero__actions {
      justify-content: center;
    }

    .hero__photo {
      width: clamp(200px, 60vw, 300px);
    }
  }
</style>
